name: flutter_laravel_pusher_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  dio: ^5.7.0
  dartz: ^0.10.1
  equatable: ^2.0.5
  get: ^4.6.6
  pusher_channels_flutter:
    path: packages/pusher-channels-flutter
#  pusher_client_fixed:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true